#boot a client that says something 
#max number of clients met
#multiple clients sending numerous messages (overload)
#sending large files
#sending too much data

import pytest
from threading import client_thread, server_thread


